# YobaLog: План работ и стратегия тестирования

## Dual-executor (инвариант тестов query engine)
- **Reference executor:** `kusto-loco`'s `KustoQueryContext` — готовое in-memory исполнение KQL над `IEnumerable<LogEvent>`. Сами не пишем — доверие к oracle выше (кто тестирует тестировщика — в нашем случае MS + NeilMacMullen).
- **Production executor:** Kusto AST → SQL через `linq2db` для SQLite+FTS5 (full-text search по Message транслируется в FTS5 MATCH). Позже — та же схема для DuckDB.
- **Property-тесты:** одна KQL-строка + фикстурный dataset прогоняются через reference и через production executor; результаты обязаны совпадать. Любое расхождение = баг трансформера.
- **Следствие:** добавление нового бэкенда требует только реализации трансформера — тесты уже готовы. Без reference executor'а каждый бэкенд тестируется отдельно, одинаковые баги пропускаются.
- **Downgrade path:** если `kusto-loco` заглохнет — переходим на голый `Kusto.Language` и пишем свой executor. Transformer'ы не меняются (Kusto AST стабилен, поддерживается MS).

## Фазы
- [x] **Фаза A.0 — Bootstrap.** Репо-гигиена и тулинг, без кода приложения. Цель — задать тон один раз, чтобы не переделывать по каждому PR.
    - [x] `.gitignore` (bin/obj, node_modules, wwwroot-генерёжка, `*.db`/`*.db-wal`/`*.db-shm`, `.vs/`, user secrets).
    - [x] `.gitattributes` — LF в сорцах, CRLF в `.cmd/.bat/.ps1`, `bun.lock` как text, `.sln` как CRLF.
    - [x] `global.json` — пин .NET 10 SDK 10.0.202 (rollForward = latestFeature).
    - [x] `Directory.Build.props` на корне: `<Nullable>enable</Nullable>`, `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>`, `<AnalysisLevel>latest-recommended</AnalysisLevel>`, `<AnalysisMode>All</AnalysisMode>`, `<EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>`, `<ImplicitUsings>enable</ImplicitUsings>`, `<LangVersion>latest</LangVersion>`, `<InvariantGlobalization>true</InvariantGlobalization>`.
    - [x] `Directory.Packages.props` — Central Package Management включён (`<CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>`), versions пока только тестовые (xunit, FluentAssertions, coverlet, Microsoft.NET.Test.Sdk).
    - [x] Solution skeleton: `YobaLog.slnx` (новый формат .NET 10) + `src/YobaLog.Core` (classlib) + `src/YobaLog.Web` (Razor Pages webapp, ссылка на Core) + `tests/YobaLog.Tests` (xunit, ссылка на Core). Шаблонные csproj очищены — общие property переехали в `Directory.Build.props`, Version из `PackageReference` убраны (CPM).
    - [x] Фронт-бутстрап рядом с `YobaLog.Web`: `package.json`, `tsconfig.json` со всеми strict-флагами + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` + `noImplicitOverride` + `noUnusedLocals` + `noPropertyAccessFromIndexSignature`, `tailwind.config.js`, заглушки `ts/admin.ts` и `ts/app.css`, `bun.lock` (текстовый, закоммичен).
    - [x] `biome.json` — lint + format для TS; табы/ширина 2 для всего включая json (json формат табы разрешает); `noExplicitAny: error`, `noNonNullAssertion: error`, `useConst: error`. `useEditorconfig` **отключён** — biome 1.9 не парсит `indent_size = tab`, а менять editorconfig нельзя (`indent_size = tab` нужен dotnet format для корректной обработки .cs). Подчинили biome своим настройкам, editorconfig остаётся авторитетом для .NET. YAML в `.editorconfig` = space-2 (табы в YAML запрещены спекой формата, не редакторами).
    - [x] MSBuild target в `YobaLog.Web.csproj`: `BeforeTargets="Build"` + `Condition="'$(Configuration)' == 'Release'"` → `bun install --frozen-lockfile && bun run build`. В Debug MSBuild не трогает фронт (dev запускается отдельно).
    - [x] CI skeleton (`.github/workflows/ci.yml`): `bun install` + `biome check` + `bun run typecheck` + `dotnet restore` + `dotnet format --verify-no-changes` + `dotnet build -c Release` + `dotnet test -c Release`. Один workflow на push/PR в main.
    - [x] Smoke-test: всё зелёное локально — `biome check` ✓, `tsc --noEmit` ✓, `dotnet format --verify-no-changes` ✓, `dotnet build -c Release` (включая `bun run build` через MSBuild target) ✓, `dotnet test` 1/1 passed ✓.
    - [x] **Cake + GitVersion + chiseled Docker + deploy-by-tag pipeline.** Синхронизирован с yobaconf (`9f99b84`). `GitVersion.yml` (next-version 0.3.0) → MSBuild `Version`/`InformationalVersion` + Docker build-args. `build.cake` оркестрирует Clean→Restore→Version→Build→Test→Docker→DockerSmoke→DockerPush; отдельный `E2ETest` task для Playwright (Chromium не тянется на каждый main-push). Dockerfile двухстадийный: SDK + bun → chiseled runtime-deps:10.0-noble. DockerSmoke пробивает `/Login` (корень под auth). CI: `test` + `e2e` параллельно → `publish` на main+`deploy` tag → `deploy` только по тегу. Trace-zip'ы от E2E аплоадятся как artifact на failure.
- [x] **Фаза A — dog-food ready.** Ingestion (`/api/events/raw`, CLEF, API-ключи, workspace routing), retention по дате (одна цифра, без политик), минимальный viewer (логин + cursor-пагинация + хардкод-фильтры: Level/Category/TraceId/time-range/substring), `$system` workspace. На этой фазе уже перенаправляется Serilog из собственных сервисов — сервис работает.
    - [x] Доменные типы в Core: `LogLevel`, `WorkspaceId` (slug-валидация), `LogEvent`, `LogEventCandidate`, `ILogStore`, `LogQuery`.
    - [x] CLEF-парсер (`CleFParser`) — tolerant NDJSON, `@t`/`@l`/`@m`/`@mt`/`@x`/`@i`/`@tr`/`@sp`, `@@`-escape, reserved `@*` skip.
    - [x] Ingestion pipeline — `Channels` per workspace, bounded + Wait, writer-loop, `IHostedService`, shutdown-drain, per-workspace isolation.
    - [x] `SqliteLogStore` — linq2db + SQLite, WAL, cursor pagination (16-байтный opaque key), индексы, FTS5 инфраструктура (для Phase C), substring через LIKE.
    - [x] API-ключи — `IApiKeyStore` + `ConfigApiKeyStore` (из `appsettings`), `X-Seq-ApiKey` header + `?apiKey=` query.
    - [x] `POST /api/events/raw` — 201 с partial-batch ack `{received, errors}`, 401 на bad/missing key.
    - [x] `WorkspaceBootstrapper` — на старте создаёт `$system` + все workspaces из API-ключей.
    - [x] Retention service — `BackgroundService`, гоняет `DeleteOlderThanAsync` per workspace; отдельный `SystemRetentionDays` для `$system`.
    - [x] Self-observability — `SystemLoggerProvider` + `SystemLogFlusher`, фильтр по префиксу категории (`YobaLog.*`), собственные логи в `$system` через прямой `ILogStore.AppendBatchAsync` (минуя pipeline, чтобы не словить рекурсию). Queue-full → drop (DropWrite).
    - [x] Минимальный viewer — Razor Pages: список workspaces (`/`), event-таблица (`/ws/{id}`) с фильтрами (message substring / min level / trace / from-to) и cursor-пагинацией. Tailwind + DaisyUI, `data-theme="dark"`. Cookie-auth с единственным админом из `appsettings` (Username+Password, constant-time сравнение через `CryptographicOperations.FixedTimeEquals`). `/api/events/raw` остался `AllowAnonymous` — API-ключи не ломаются. Протестировано end-to-end через Playwright MCP (Edge): unauth → redirect на `/Login`, wrong creds → alert, correct → `ReturnUrl` ведёт обратно, logout чистит cookie.
    - [x] **Deploy-wiring под Caddy-based HTTPS (spec §11).** `UseForwardedHeaders` c `KnownProxies = { IPAddress.Loopback, IPAddress.IPv6Loopback }` в `YobaLogApp.Configure()` — **перед** `UseHttpsRedirection`. `KnownIPNetworks`/`KnownProxies` очищаются перед добавлением, чтобы доверять только loopback (forged `X-Forwarded-Proto` из non-proxy источника → ignored). Integration-тест `ForwardedHeadersTests` (в E2E, нужен real Kestrel — TestServer ставит `RemoteIpAddress=None`): POST `/api/ws/{ws}/share` с `X-Forwarded-Proto: https` → URL в JSON-ответе начинается с `https://` (scheme прокидывается через FH → `ctx.Request.Scheme` → share-URL template).
    - [x] **First-time host bootstrap doc (`doc/deploy.md`).** Пошаговая инструкция: DNS A-запись `yobalog.3po.su`, `apt install caddy` (из cloudsmith apt-репо), scaffold `/etc/caddy/Caddyfile` из `infra/Caddyfile.fragment`, `ufw allow 80,443/tcp`, `systemctl enable caddy`, GitHub secrets, первый `git tag deploy`. Отдельные секции: pre-requisites, regular-flow для последующих релизов, добавление следующего сервиса под Caddy (yobaconf next — пропускает host-setup, только append fragment + `systemctl reload caddy`), troubleshooting (502, redirect loop, cert-не-выпускается, SSE batching, share-URL с http).
    - [x] **SSE-compat fallback guard.** Полный Caddy-в-testcontainers E2E тест не окупается сейчас (heavy setup, rare regression vector) — вместо него 3 unit-теста на сам fragment (`CaddyfileFragmentTests`): (1) `flush_interval -1` присутствует, (2) `reverse_proxy` указывает на `127.0.0.1:8082`, (3) host-block — `yobalog.3po.su`. Ловит silent-drop строки при будущем rewrite'е `infra/Caddyfile.fragment`. Full Caddy-E2E поднимается в Phase F/H если появится реальная регрессия на SSE-стриминге.
- [~] **Фаза B — transformer и dual-executor тесты.** Parser/AST/reference executor — готовые (`Kusto.Language` + `kusto-loco`).
    - [x] Visitor по Kusto AST → LINQ expression tree (через `linq2db` → SQL для SQLite).
    - [x] Операторы: `where`, `take`, `order by` (single + multi, asc/desc).
    - [x] Предикаты `where`: `==`, `!=`, `<`, `<=`, `>`, `>=`, `contains` (case-insensitive), `and`, `or`, `not()`.
    - [x] Колонки: `Id` (long), `Level` (int rank 0..5), `LevelName` (string), `Timestamp` (datetime literal → Unix ms), `TraceId`, `SpanId`, `Message`.
    - [x] Dual-executor тесты — 34 property-случая, reference = `KustoQueryContext`, production = `IQueryable<EventRecord>`.
    - [x] `SqliteLogStore.QueryKqlAsync` — реальный SQL через linq2db, 7 integration-тестов.
    - [x] `project`, `extend`, `summarize`, `count` — `KqlResult { Columns, Rows }` как loose-typed result, `Execute` разбивает pipeline на SQL-pushdown prefix (where/take/order) + in-memory shape-changing suffix. MVP scope: `project <col|alias=col>`, `extend <alias=col>`, `count`, `summarize count() [by <cols>]`. Computed expressions + другие агрегаты throw с actionable message. Viewer пока использует старый `QueryKqlAsync` (event-shape), миграция на `QueryKqlResult` + generic renderer — когда появится usecase.
    - [x] FTS5 MATCH — добавлен KQL-оператор `has` для `Message`. Транслятор детектит `Message has '...'` и зовёт `KqlSqlExpressions.FtsHas(id, message, term)` с `[Sql.Expression("{0} IN (SELECT rowid FROM EventsFts WHERE Message MATCH {2})", IsPredicate=true)]` — linq2db рендерит FTS5-subquery, использует indexed MATCH вместо full scan. C#-fallback через word-boundary Tokenize (для in-memory/dual-executor). `has_cs` не поддержан (FTS5 default tokenizer case-insensitive). `contains` по-прежнему LIKE full scan по spec §7. 3 integration-теста + 4 dual-executor случая.
    - [ ] Timestamp в dual-executor — kusto-loco странно обрабатывает `datetime()`-литерал; production корректен, reference закомментирован.
    - [x] Unsupported operators/predicates → explicit `UnsupportedKqlException` с actionable message.
- [x] **Ingestion URL namespace split** (не привязано к фазе, инфраструктурная правка). Нативный канонический путь — `POST /api/v1/ingest/clef`; Seq-compat вынесен из корня в `/compat/seq/api/events/raw` (Seq-клиенты жёстко дописывают `/api/events/raw` к base URL, который пользователь пропишет как `https://<host>/compat/seq`). `IngestionHandlers.CleF` общий, `ResolveScopeAsync` + `IIngestionPipeline.IngestAsync` шарятся. Будущие форматы — два вектора: `/api/v1/ingest/<fmt>` для нативных (gelf, otlp) и `/compat/<tech>/...` для вендор-совместимых (hec, statsd). Детали в `decision-log.md` (2026-04-19 "Ingestion namespace split").

- [x] **Фаза C — swap на KQL.** Viewer прозрачно переехал на KQL: форма та же, page-model билдит KQL-строку и гоняет через `SqliteLogStore.QueryKqlAsync`. Pagination через `where Timestamp < cursor or (Timestamp == cursor and Id < id)`. Протестировано через Playwright end-to-end (55 событий, 2 страницы, фильтры сходятся).
    - [x] Form → KQL builder в `WorkspaceModel.BuildKql()` — from/to/minLevel/trace/message/cursor.
    - [x] `ILogStore.QueryKqlAsync` — часть контракта; `SqliteLogStore` реализует.
    - [x] `<textarea>` с сырым KQL — collapsible под формой. Когда непусто → форма дизейблится, `RawKql` летит в page-model. Pager (order by, take, cursor) всегда добавляется на сервере. Parse-ошибки и `UnsupportedKqlException` рендерятся красным alert'ом с сохранением пользовательского ввода.
    - [x] Server-side автокомплит через htmx + `KustoCodeService.GetCompletionItems`. Endpoint `GET /api/kql/completions?q=...&pos=...` возвращает HTML-fragment с дропдауном. Схема `events` зарегистрирована в `GlobalState` — completions знают про Id/Timestamp/Level/LevelName/TraceId/SpanId/Message/Exception. Insert-on-click через minimal TS (`ts/admin.ts`): замена EditRange → BeforeText+AfterText, caret между ними, Escape закрывает панель. Live-проверено: `Le` → {Level, LevelName}; после `|` → {where, count, extend, join, …}; click вставляет `LevelName ` с пробелом-разделителем.
    - [x] Saved queries CRUD — `ISavedQueryStore` + `SqliteSavedQueryStore`, `.meta.db` per workspace (spec §6). Upsert by name, List ordered. UI: chip-list с delete-кнопками, "Save as…"-форма, клик по chip → `?saved=<name>` подгружает KQL. Live-проверено.
    - [x] Retention-политики с ссылками на saved queries — `RetentionPolicy { Workspace, SavedQuery, RetainDays }` список в конфиге. Если у workspace есть policies, применяются они (каждая = saved query filter + Timestamp < cutoff); если нет — fallback `DefaultRetainDays`. `$system` всегда по `SystemRetainDays`. Missing saved query → warn + skip. Новый `ILogStore.DeleteKqlAsync(WorkspaceId, KustoCode)` через transformer → linq2db DELETE. 3 новых теста (+4 обновлённых).
- [x] **Фаза D — KQL-first UI + админка.** Интерфейс, который заточен под KQL вместо формы фильтров, и DB-backed CRUD для админских сущностей вместо конфига.
    - [x] **KQL-first UI.** Филтр-поля (Message/Level/Trace/From/To) вынесены из разметки ещё в Phase C-свапе; `WorkspaceModel` держит только `kql` + `saved` + `cursor` `[BindProperty(SupportsGet)]`. Единственный ввод в `/ws/{id}` — KQL textarea с автокомплитом через htmx. Остаточная `<form method="get">` — просто контейнер для submit; фильтр-inputs отсутствуют.
    - [x] **Hover-фильтры в таблице событий.** Над ячейкой при наведении — всплывают ✓ и ✗ (for Timestamp — from/to, inclusive `>=`/`<=` чтобы текущая строка оставалась). Markup в `_EventRow.cshtml` + expanded row через partial `_EqNeChips`. Click-handler в `admin.ts` ловит `[data-filter-field]`, дописывает `| where {Field} {sym} {value}` в textarea и вызывает `form.requestSubmit()`. MVP append-only; smart-merge (заменять существующий предикат по тому же полю) — отложено до появления дискомфорта. E2E `HoverChipsTests.Click_Level_eq_chip_appends_where_predicate_and_filters` фиксирует поведение.
    - [x] **Админка через UI.** Было всё config-driven: workspaces косвенно через `ApiKeys.Keys[].Workspace`, API-ключи plain в `ConfigApiKeyStore`, retention policies массивом в `Retention.Policies`. Переехали на DB-бэкед стораджи в `$system.meta.db`:
        - [x] `IWorkspaceStore` + CRUD-страница `/admin/workspaces`. Stored в `$system.meta.db`, таблица `Workspaces(Id TEXT PK, CreatedAtMs)`. `WorkspaceBootstrapper` на первом старте мигрирует `ApiKeys.Keys[].Workspace` → DB (если store пустой); дальше — single source of truth. Delete дропает log-store + meta через `ILogStore.DropWorkspaceAsync`. `$system` из листинга скрыт, delete на него = `InvalidOperationException`.
        - [x] `IApiKeyStore` — двухуровневый стек: `ConfigApiKeyStore` (plaintext в `appsettings.json`, admin backdoor без UI — master-ключ на прописанные в конфиге workspaces) остаётся + `SqliteApiKeyStore` (per-workspace `.meta.db` рядом с saved queries / share / masking, `sha256(token)` + 6-символьный префикс, plaintext показывается один раз). `CompositeApiKeyStore` на hot-path первой валидирует Config, затем Sqlite; `ConfiguredWorkspaces` = union. CRUD на `/ws/{id}/admin/api-keys` (workspace-scoped), token = `ShortGuid` (22 симв, 122 бита). `SqliteWorkspaceStore.CreateAsync/DeleteAsync` зовёт `IApiKeyAdmin.InitializeWorkspaceAsync/DropWorkspaceAsync` — meta-таблица создаётся при регистрации workspace'а, при удалении `.meta.db` исчезает целиком.
        - [x] **Ingestion endpoints** на странице workspace (`/ws/{id}`) — `<details>`-блок со списком поддерживаемых входов. Сейчас одна строка: Seq-compat base URL `https://<host>/compat/seq` (Seq-клиенты сами допишут `/api/events/raw`). Слоты под будущие `/compat/gelf`, `/compat/hec`, `/api/v1/ingest/clef` добавятся рядом.
        - [x] `IRetentionPolicyStore` + `/admin/retention` — CRUD полиси per workspace. `SqliteRetentionPolicyStore` в `$system.meta.db`, таблица `RetentionPolicies(Workspace, SavedQuery, RetainDays)` с composite PK `(Workspace, SavedQuery)`. `RetentionService.SweepWorkspaceAsync` читает `_policyStore.ListByWorkspaceAsync` → если пусто, падает на config `Retention:Policies[]` → если и там пусто, sweep по `DefaultRetainDays`. Admin-страница: workspace-select (из `IWorkspaceStore`, `$system` скрыт) + saved-query-text + days, delete через confirm. **Блокировка удаления saved query**: `WorkspaceModel.OnPostDeleteAsync` смотрит `ListByWorkspaceAsync`, если имя используется — `FlashError` "used by retention policy; delete it first" + redirect, без удаления. `[TempData] FlashError` рендерится наверху Workspace-страницы.
        - [x] `IUserStore` + `/admin/users` — мульти-админ. `SqliteUserStore` в `$system.meta.db`, таблица `Users(Username PK, PasswordHash, CreatedAtMs)`. Хеши через `AdminPasswordHasher` (PBKDF2-HMAC-SHA256, 600k iter). Dummy-verify на miss-path для constant-time защиты от user-enumeration по тайминам. `LoginModel.AuthenticateAsync`: если `IUserStore.ListAsync` пуст → config-admin fallback; иначе только DB (config игнорируется до удаления последнего DB-юзера). CRUD на `/admin/users`: create, rotate password, delete через confirm dialog.
        - [x] **Antiforgery на Login** включён — `[IgnoreAntiforgeryToken]` убран с `LoginModel`. Форма автоматически рендерит `__RequestVerificationToken`; HttpClient-тесты (ShareEndpointTests, LiveTailTests, ShareLinkTests, обновлённый YobaLog.Tests.LoginTests) делают GET /Login → парсят токен из формы → POST с токеном и antiforgery-cookie. `HttpAuthHelper.AuthenticatedClientAsync` вынесен в Infrastructure — все HTTP-level тесты зовут его.
- [~] **Фаза E — usability-полировка.** Share links + маскирование UI (готово); infinite scroll (готово); live-tail polish.
    - [~] **Live tail / auto-refresh:** базовый контур готов. `ITailBroadcaster` + `InMemoryTailBroadcaster` (Channel per-subscriber, Bounded DropOldest, KQL-фильтр per-subscription — publish общий на workspace, каждый subscriber прогоняет через compiled `KqlTransformer.Apply`). `ChannelIngestionPipeline` публикует батчи после успешного AppendBatchAsync. `GET /api/ws/{id}/tail?kql=<>` SSE-endpoint через `RazorPartialRenderer` → partial `_EventLiveRow.cshtml` (single source of truth для row markup). Viewer: toggle "Live tail" динамически создаёт `<div hx-ext="sse" sse-connect=...>`, htmx-ext-sse делает `afterbegin` swap в `#events-body`. CSS `event-live` → flash-animation (DaisyUI primary-color fade 1.4s). Shape-changing KQL (project/summarize/count/extend) rejected at subscribe. Проверено через Playwright. **Остаётся:** coalescing (N-ms окна), rate cap + drop marker, viewport awareness ("N новых" индикатор), reconnect at filter-change, persistence window (не per-subscriber 100, а скользящий общий).
    - [ ] **Viewport awareness:** пока пользователь не наверху — новые события не префендятся, накапливаются в индикаторе "N новых"; клик/скролл к верху применяет.
    - [x] **Infinite scroll (вместо "Load older"):** sentinel-row в конце `<tbody>` с `hx-trigger="intersect once"` фетчит следующую страницу через ту же `/ws/{id}` URL. `WorkspaceModel.OnGetAsync` при `HX-Request` возвращает `Partial("_RowsFragment", this)` — те же rows + новый sentinel. htmx 2.0 `outerHTML`-swap на `<tr>` использует template-fragments автоматически. Copy/expand handlers в `admin.ts` делают event-delegation на `document`, так что работают на новых строках без `htmx.process`. Верхняя граница (cursor-based "show newer") — когда появится use-case. Spec §7 запрещает offset.
    - [x] **Share → masked TSV** per spec §3. DB-backed links: `ShareLink(Id, Kql, CreatedAt, ExpiresAt, Salt, Columns, Modes)` в `.meta.db` per workspace, Id — `ShortGuid` (22-char base64url от Guid.NewGuid). `GET /share/{ws}/{id}.tsv` anonymous, 404 для неизвестного id, 410 Gone + lazy-delete при expiry. `RetentionService` сметает протухшие ссылки заодно с событиями. Три режима на поле: `keep` / `mask` (детерминированный HMAC-SHA256(salt,value)→4-byte hex с префиксом из последнего сегмента пути — `email:a1b2c3d4`, связи в рамках одной ссылки сохраняются) / `hide` (поле вырезается, column тоже). Property keys — плоский namespace с top-level, top-level shadows properties на collision. Policy-per-workspace в `FieldMaskingPolicy` — модалка не переспрашивает одно и то же. Revocation — `DELETE` row (UI пока нет). HTML-preview шарированного view отложен — 99% use-case TSV-to-LLM.
- [~] **Фаза F — OTLP logs ingestion.** Решения зафиксированы в `decision-log.md` (2026-04-21, промоутированы в `spec.md` §1-2). Любой OTel-enabled .NET/Go/Python/JS app становится yobalog-писателем без адаптерного кода. Mirror Seq-surface: `/ingest/otlp/v1/logs` + OTel-стандартный `/v1/logs` (alias), HTTP/Protobuf, `X-Seq-ApiKey` header/query. gRPC и HTTP/JSON отложены.
    - [x] `OpenTelemetry.Proto` NuGet **не существует** (проверено 2026-04-21 — ни в nuget.org, ни в `OpenTelemetry.Exporter.OpenTelemetryProtocol` public types). Вендорим 4 `.proto` файла из `opentelemetry-proto v1.5.0` в `src/YobaLog.Web/Proto/`, `Grpc.Tools` компилирует их в C# во время MSBuild. `Google.Protobuf` + `Grpc.Tools` (PrivateAssets) в `Directory.Packages.props`. `YobaLog.Web.csproj` ссылается; `YobaLog.Core` — нет (invariant: proto-типы wire-boundary only, не утекают в `ILogStore`).
    - [x] `OtlpLogsParser` — static class в `YobaLog.Web.Ingestion/`. `Parse(ReadOnlySpan<byte>) → OtlpLogsParseResult(Candidates, Errors, IsMalformed)`. Маппинг полей по таблице из decision-log (severity 1-24 → 6-уровневая лестница, body-типы, resource-wins-on-collision для attributes, trace_id/span_id hex-encoding с skip-на-all-zero, event_name → @mt, fallback observed_time_unix_nano, drop на обоих timestamp=0). 18 unit-тестов на `OtlpLogsParserTests`.
    - [x] `IngestionHandlers.OtlpLogs` handler + два `MapPost` в `YobaLogApp.MapEndpoints`: `/ingest/otlp/v1/logs` (Seq-mirror) и `/v1/logs` (OTel-standard alias) — оба `AllowAnonymous`, auth через `ResolveScopeAsync` (`X-Seq-ApiKey` header или `?apiKey=`). 6 integration-тестов на `OtlpIngestionEndpointTests` (parametrized на оба пути).
    - [x] Compat-тест `OtlpDotnetCompatTests` в `YobaLog.E2ETests/Compat/`. Использует `OpenTelemetry.Exporter.OpenTelemetryProtocol` 1.15.1 (`Simple` processor для test determinism) — крутит реальный OTel-stack против `KestrelAppHost`, ассертит state store. 2 теста (valid flow + wrong-key negative). Pattern отzeркален с `SerilogSeqSinkCompatTests`.
    - [ ] `OtlpPythonCompatTests` (Python + `opentelemetry-exporter-otlp-proto-http`). Pattern будет зеркалить `WinstonSeqCompatTests` (bun external process). Отложено — .NET-compat-тест уже покрывает wire-level-корректность; Python-версия нужна только чтобы ловить SDK-specific quirks. Добавится при реальном Python-клиенте.
    - [ ] Дифф в `perf-baseline.md`: BDN-бенчмарк OTLP-parse vs CLEF-parse на равных батчах. Отложено до появления hot-path-давления — пока parser явно не узкое место.
- [~] **Фаза G — OTel self-emission.** Landed 2026-04-21. Промоутнуто в `spec.md` §8.
    - [x] Пакеты `OpenTelemetry` 1.15.1, `.Extensions.Hosting` 1.15.1, `.Instrumentation.AspNetCore` 1.12.0, `.Instrumentation.Http` 1.12.0 — в `Directory.Packages.props`. Референсятся только Web (Core не ссылается; Core использует только `System.Diagnostics.ActivitySource` из BCL).
    - [x] `AddOpenTelemetry().WithTracing(...)` в `YobaLogApp.ConfigureServices`, gated на `!IsEnvironment("Testing")` (паттерн `UseHttpsRedirection`). Slushaет все 4 источника + AspNetCore (с filter'ом на `/health`+`/version`) + HttpClient. Processor — `SimpleActivityExportProcessor(SystemSpanExporter)`.
    - [x] Named `ActivitySource`'ы в `YobaLog.Core/Observability/Tracing.cs`: `YobaLog.Ingestion`, `YobaLog.Query`, `YobaLog.Retention`, `YobaLog.Storage.Sqlite`. Instrumentation sites: `ChannelIngestionPipeline.IngestAsync`, `WorkspaceModel.OnGetAsync` (query branch), `RetentionService.RunPassAsync`, `SqliteLogStore.AppendBatchAsync/QueryKqlAsync/DeleteOlderThanAsync/DeleteKqlAsync`. Все storage-сайты check `if (workspaceId.IsSystem) return null;` перед `StartActivity` — не создают спан для $system-записей (иначе экспортёр рекурсит).
    - [x] `SystemSpanExporter : BaseExporter<Activity>` в `YobaLog.Web.Observability/`. Flatten Activity → `LogEventCandidate { Kind="span", Name, Source, StartUnixNs, DurationNs, ParentSpanId, StatusCode, ...tags }` + `TraceId`/`SpanId` как поля. Пишет в `$system` через `ILogStore.AppendBatchAsync` напрямую (минует `IIngestionPipeline`, как `SystemLoggerProvider`). Reserved-key shadowing-guard на тегах.
    - [x] Skip-filter на `/health` + `/version` в `AspNetCoreInstrumentationOptions.Filter`.
    - [x] Tests: 11 unit `SystemSpanExporterTests` (маппинг полей + tag-flattening + reserved-key guard + Export-path) + 2 E2E `SelfEmissionTests` (real ingest → spans land in $system via local ActivityListener + exporter; confirms $system не instrumentируется через control-test).
    - [ ] BDN regression: baseline vs +OTel на SqliteLogStore + IngestionPipeline — gate ±10% timing, ±1% allocations. Отложено — прогон делать вручную когда накопятся значимые изменения в hot-path'ах. Activity overhead без listener'а ~10 ns (негомогенно low), с listener'ом 0.5-2 μs per span на batch-boundary — укладывается в бюджет decision-log.
- [x] **Фаза H — trace ingestion + waterfall UI.** Разбита на 3 коммита, все landed 2026-04-21: **H.1 foundation** (ISpanStore + SqliteSpanStore + exporter migration); **H.2 OTLP Traces ingestion** (/v1/traces + /ingest/otlp/v1/traces); **H.3 KQL spans target + waterfall UI** (`spans | where | order | take` + `/trace/{id}` Razor).
    - [x] **H.1 Schema-decision (b2) — отдельный файл `{workspace}.traces.db`** (decision-log 2026-04-21): `Spans(SpanId PK, TraceId, ParentSpanId, Name, Kind, StartUnixNs, EndUnixNs, StatusCode, StatusDescription, AttributesJson, EventsJson, LinksJson)`, composite index `(TraceId, StartUnixNs)` для waterfall + standalone `StartUnixNs` для retention. Выбор файла-не-таблицы обоснован измеренным 66× query-latency penalty в `perf-baseline.md` (shared writer-lock per SQLite file). `ISpanStore` contract + `SqliteSpanStore` impl в `src/YobaLog.Core/Tracing/`.
    - [x] **H.1 SystemSpanExporter switched to ISpanStore.** Phase G был на rich-logs в `$system.logs.db` с `Properties.Kind="span"`; теперь пишет в `$system.traces.db` через `ISpanStore.AppendBatchAsync` с structurally typed полями (SpanId / TraceId / Duration — first-class). Старые rich-log spans в `$system.logs.db` остаются навсегда (migration `/admin/migrate-spans` — отдельный tech-debt тикет, если кому-то важно).
    - [x] **H.1 Retention + bootstrap wiring.** `RetentionOptions.DefaultSpansRetainDays` / `SystemSpansRetainDays` — асимметричный retention "логи 30d / трейсы 7d" теперь работает из коробки. `RetentionService.RunPassAsync` дополнительно сметает `ISpanStore.DeleteOlderThanAsync` для user workspaces + `$system`. `WorkspaceBootstrapper` + `SqliteWorkspaceStore.CreateAsync/DeleteAsync` создают/дропают `.traces.db` параллельно с `.logs.db`. DI в `YobaLogApp`: `ISpanStore → SqliteSpanStore` singleton.
    - [x] **H.1 Tests.** 7 integration-тестов `SqliteSpanStoreTests` (append → getByTraceId round-trip + retention + workspace isolation via separate `.traces.db` files + drop). 10 unit `SystemSpanExporterTests` обновлены на Activity → Span маппинг. 2 E2E `SelfEmissionTests` обновлены: реальный ingest → real spans в `$system.traces.db` + round-trip через `GetByTraceIdAsync`.
    - [x] **H.2 OTLP Traces ingestion.** Vendored `trace/v1/trace.proto` + `collector/trace/v1/trace_service.proto` из opentelemetry-proto v1.5.0 (Grpc.Tools компилирует в build'е). `OtlpTracesParser` в `YobaLog.Web.Ingestion/`: mapping по decision-log 2026-04-21 — trace/span/parent IDs hex lowercase, all-zero parent = null, OTLP `SpanKind` 1-indexed + Unspecified → domain `SpanKind` 0-indexed (explicit switch для exhaustiveness, direct cast был бы смещением на 1); resource.attributes + scope.attributes + span.attributes merged с resource-wins-on-collision; events/links/status preserved. `POST /v1/traces` + `POST /ingest/otlp/v1/traces` — оба AllowAnonymous, auth через `X-Seq-ApiKey` header или `?apiKey=` query. Пишет в `ISpanStore.AppendBatchAsync`. Tests: 25 unit `OtlpTracesParserTests` (каждая строка маппинга + partial-batch + malformed + reject на all-zero IDs / zero start_time), 6 integration `OtlpTracesEndpointTests` (оба пути через `[Theory]`, auth variants, garbage body), 2 real-SDK compat `OtlpDotnetTracesCompatTests` (TracerProvider + OtlpExporter → /v1/traces → parent/child span tree roundtrip через GetByTraceIdAsync + wrong-key negative).
    - [x] **H.3 KQL `spans` target + waterfall UI.** KQL: вместо генерализации 637-строчного `KqlTransformer` сделан отдельный `KqlSpansTransformer` (~200 LOC) scope'нутый на spans — support'ит `where / order by / take` с typed predicates (SpanId/TraceId/ParentSpanId/Name string, Kind/Status int, StartTime/EndTime datetime, Duration computed int ns). Project/extend/summarize/count над spans target deferred (waterfall UI идёт через `GetByTraceIdAsync`, KQL над spans — для filter-sort-limit паттернов "recent long spans" / "spans in trace"). `SqliteSpanStore.QueryKqlAsync` wired up через transformer. 16 unit `KqlSpansTransformerTests`. UI: `Trace.cshtml` + `/trace/{id}?ws=<slug>` + `_TraceWaterfall.cshtml` partial — bars с width ∝ Duration (min 0.5% tick для zero-duration), indent по parent-depth через BFS-cached map, цвет по StatusCode (Ok/Error/Unset). Click-to-expand details row через event-delegated TS в `admin.ts`. Zero JS deps сверх htmx. 2 E2E `TraceWaterfallTests` (3-span seed roundtrip + unknown-trace 404 alert).
    - [x] **H.4 Traces listing UI** (закрыт 2026-04-22). `/ws/{id}/traces` — таблица recent traces с aggreгатами: TraceId · root-span name · start time · duration · span count · status. Cursor pagination через base64-encoded `(StartUnixNs, TraceId)` (newer-first). `ISpanStore.ListRecentTracesAsync(workspaceId, TracesQuery, ct)` + новый `TraceSummary` record + `TracesQuery` cursor record. SQL: `GROUP BY TraceId` aggregate (min/max/count/worst-status) + сепарат-pass для root-span name (`ParentSpanId IS NULL` first, else earliest start). Workspace page получил кнопку "Traces" в header. 3 unit `SqliteSpanStoreTests` (aggregate ordering, cursor pagination, empty-store) + 3 E2E `TracesListingTests` (rows render + click → waterfall, empty state, nav link from `/ws/{id}`). Waterfall теперь reachable из UI без копания в SQLite на VM.
    - [x] **H.4 extension — KQL search + incremental auto-refresh.** Feature parity с events UI. `TracesQuery` дополнен `Filter (KustoCode)` + `SinceStartUnixNs` полями; `SqliteSpanStore.ListRecentTracesAsync` применяет `KqlSpansTransformer.Apply` перед GROUP BY (span-level filter — trace shown, если любой его span matches) + WHERE на aggregate для since-cursor. UI: KQL textarea + Apply button над таблицей; `_TracesRows` partial с `data-start-unix-ns` на каждой row'е. htmx `<tbody hx-trigger="every 5s" hx-swap="afterbegin" hx-vals='js:{since: <topmost-row-dataset>, kql: <textarea-value>}'>` — сервер возвращает `_TracesRows` partial при `HX-Request`, prepend'ит только новые traces. Existing rows не ре-рендерятся, scroll не прыгает, фильтр respects'ится poll'ом. Parse-ошибки KQL → alert с сохранением ввода. 2 новых unit (filter + since branches) + 3 новых E2E (KQL narrows rows, parse-error alert, poll markup attributes).
    - [ ] **Service map — explicit defer** (decision-log 2026-04-21 "out of MVP scope").
- [ ] **DuckDB backend (отложено, внешний blocker).** Вторая реализация `ILogStore` после мёрджа [linq2db#5451](https://github.com/linq2db/linq2db/pull/5451). Transformer пишется минимально (SQL с поправками на DuckDB-диалект), dual-executor тесты покрывают автоматически. Timeline зависит от ревью PR в upstream; номер фазы не присвоен чтобы не блокировать ordering OTel-фаз F/G/H.

## UI regression coverage

Playwright MCP (интерактивно, через Claude) — для smoke-проверки новой функциональности при разработке. Для CI-регрессий нужен скриптовый слой. Selenium-обвязки (Atata, SpecFlow+WebDriver) — прошлый век: WebDriver-протокол медленнее CDP/BiDi, флакier; Atata добавляет фреймворк поверх уже проблемного стека. Используем **`Microsoft.Playwright`** (.NET SDK) — та же библиотека, что у MCP, но скриптами. Auto-waiting из коробки, trace viewer для дебага CI-падений, в 3-5× быстрее Selenium.

- [x] **Тест-проект `tests/YobaLog.E2ETests/`.** xUnit + `Microsoft.Playwright 1.59.0`. Browsers через `pwsh bin/Debug/net10.0/playwright.ps1 install chromium`; в CI отдельный job с `--with-deps`. Chromium-only — htmx+SSE+стандартный DOM, cross-browser не окупается. Объединён с Kestrel-based compat тестами (`WinstonSeqCompatTests` + `SerilogSeqSinkCompatTests` переехали из `YobaLog.Tests`) — общий `KestrelAppHost` бутстрап. UI-классы через `ICollectionFixture<WebAppFixture>` — один Kestrel + один Chromium на весь UI-слой.
- [x] **Host-фабрика `KestrelAppHost`.** `WebApplication.CreateBuilder` с Kestrel на `http://127.0.0.1:0`, fresh temp data-dir per fixture. WebApplicationFactory не используется — он hard-код-ит TestServer, а браузер с in-memory handler не работает. Два ключевых гвоздя: (1) `WebApplicationOptions.ApplicationName = YobaLog.Web` — иначе entry-assembly test-dll, Razor Pages discovery не видит `.cshtml`, authz-fallback редиректит /Login → /Login. (2) `EnvironmentName = "Testing"` — `YobaLogApp.Configure` под этим флагом не вызывает `UseHttpsRedirection` (иначе http-only Kestrel зацикливается 307-ми). Seed данных через `ILogStore.AppendBatchAsync` напрямую. `WebRootPath` указывает на `src/YobaLog.Web/wwwroot` через `AssemblyMetadata("YobaLogWebProjectDir")` — без этого `StaticFileMiddleware` warn-ит.
- [x] **Селекторы = `data-testid`, без привязки к тексту или CSS.** См. инвариант в `AGENTS.md`: user-facing строки — цели локализации (§9 спеки), CSS-классы — стилевая деталь. Разметка получает `data-testid="kql-input"`, `data-testid="kql-apply"`, `data-testid="events-row"` и т.п. на всех элементах, которые трогают тесты. Слаги — английский kebab-case, стабильны к переводу.
- [x] **Page objects — hand-rolled, без фреймворков.** Тонкий слой классов (`LoginPage`, `WorkspacePage`) с методами `GotoAsync`, `SubmitKqlAsync(string kql)`, `AssertRowCountAsync(int n)`. Внутри — только `page.GetByTestId(...)`; никаких `GetByText`, `GetByRole(Name=...)`, CSS-класс-селекторов. `HasText=...` разрешён только внутри testid-scoped локатора и только для проверки data-контента (сообщение события, имя saved query), никогда — на chrome. Пример:

    ```csharp
    public sealed class WorkspacePage(IPage page)
    {
        public async Task SubmitKqlAsync(string kql)
        {
            await page.GetByTestId("kql-input").FillAsync(kql);
            await page.GetByTestId("kql-apply").ClickAsync();
        }

        public Task AssertRowCountAsync(int n) =>
            Expect(page.GetByTestId("events-row")).ToHaveCountAsync(n);

        public async Task AssertMessagesAsync(params string[] messages)
        {
            var rows = page.GetByTestId("events-row");
            foreach (var m in messages)
                await Expect(rows.Filter(new() { HasText = m })).ToBeVisibleAsync();
        }

        public Task AssertKqlErrorAsync(string text) =>
            Expect(page.GetByTestId("kql-error")).ToContainTextAsync(text);
    }
    ```
- [x] **Golden paths (must-cover):** все 18 E2E-тестов зелёные (8 UI-flows + 3 admin + 3 htmx-wiring + 3 Serilog/Winston compat + 1 SSE-contract).
    - [x] Login: wrong creds → alert; right → workspace-list.
    - [x] Workspace KQL: filter применяется; parse-error показывается красным.
    - [x] Logout чистит cookie.
    - [x] Cursor pagination: seed 60 → первая страница 50 + sentinel; навигация по sentinel href → оставшиеся 10. **Плюс** htmx-вариант — настоящий intersect trigger через scroll.
    - [x] Live tail: server contract (HttpClient streaming → broadcaster.Publish → SSE frame). **Плюс** htmx-вариант — toggle → admin.ts строит sse-connect → htmx-ext-sse открывает стрим → событие префендится в #events-body.
    - [x] Saved queries: apply KQL → save с именем → chip появляется → click по chip перезагружает KQL.
    - [x] Share: generate через `POST /api/ws/{id}/share` → anonymous TSV fetch содержит данные; expired link → 410, lazy-delete; второй fetch → 404. Модалка share тестится вручную через MCP.
    - [x] KQL completions: `events | where Lev` → debounced GET → #kql-completions показывает Level/LevelName.
    - [x] Admin workspaces: create через форму → row появляется → навигация в новый ws работает (regression на bug a3acf78) → delete через confirm dialog → .db файлы удалены.
    - [x] Admin API keys: create показывает plaintext один раз → тот же plaintext валидирует POST /compat/seq/api/events/raw → delete → ingest 401.
- [x] **Local htmx fixture.** `tests/YobaLog.E2ETests/Fixtures/htmx/htmx.min.js` + `htmx-ext-sse.js` закоммичены, Playwright route `**/unpkg.com/**` теперь `FulfillAsync` локальным файлом вместо пустого body. Пинованы на версии из `_Layout.cshtml` (2.0.4 + 2.2.3) — бамп Layout = бамп fixture.
- [x] **Storage-state auth.** Фикстура логинится один раз, сохраняет cookie через `context.StorageStateAsync` → `StorageStatePath`, каждый тест получает authenticated context. Без этого login на каждый test-method → медленнее + флейкит. Тесты login flow явно берут `authenticated: false`.
- [x] **Disable per-assembly parallelization + single UI fixture.** `[assembly: CollectionBehavior(DisableTestParallelization = true)]` + `ICollectionFixture<WebAppFixture>` для UI-классов. Множественные Kestrel+Chromium пары гонялись на старте. Compat-тесты остаются per-class (каждый свой `KestrelAppHost`) — у них разные ApiKeys/Workspace, шаринг добавил бы сложности без выигрыша.
- [x] **Block external CDN in test contexts.** `<script src="https://unpkg.com/htmx...">` в `_Layout.cshtml` — blocking tag. DNS до unpkg.com внутри headless Chromium иногда висел 15-30с, останавливая DOM-парсер до этого скрипта (симптом: dump страницы обрывается на первом script, body пустой). `context.RouteAsync("**/unpkg.com/**", fulfill empty)` отбивает внешние запросы на уровне Playwright. Тесты не проверяют клиентский htmx; когда появятся live-tail/htmx тесты — переключить конкретные роуты на локальные копии htmx.
- [ ] **Skip:** pixel-perfect / CSS regression; animation-тайминги (flash в live-tail); cross-browser.
- [x] **Trace-on-failure.** `TraceArtifact` хелпер: `StartAsync(ctx)` на каждый `WebAppFixture.NewContextAsync()`, `StopAndSaveAsync(ctx, ITestOutputHelper)` в `DisposeAsync` тест-класса. Имя теста извлекается из private `test` field'а у `ITestOutputHelper` через reflection → zip кладётся в `bin/<cfg>/<tfm>/artifacts/<Class.Method>.zip`. Always-save pattern (xUnit 2.x в `DisposeAsync` не отдаёт test outcome); в CI — `upload-artifact: if: failure()` оставит только зелёные runs без артефактов. Локально — `artifacts/` в `.gitignore`, удалять вручную при необходимости. Trace пишется только для тест-контекстов; seed-login в фикстуре явно обходится через `trace: false` чтобы не плодить мусор.
- [ ] **CI-бюджет.** Полный прогон ≤3 минут на обычном GitHub Actions runner. Если разрастётся — parallel workers через xunit `[CollectionDefinition(DisableParallelization = false)]`, шардинг admin/viewer/ingestion.
- [ ] **Phase placement.** После закрытия Фазы D (админ-UI стабилизируется), перед тяжёлыми back-end-работами (DuckDB / OTel-ingest). Раньше — будем переписывать тесты на каждом изменении админки.

## Perf / регрессии
См. [`performance-testing.md`](performance-testing.md) для философии и tier'ов, [`perf-baseline.md`](perf-baseline.md) для актуальных чисел.
- [x] BDN-проект `benchmarks/YobaLog.Benchmarks/` + стартовые бенчи (CleFParser, KqlTransformer на всех операторах, SqliteLogStore ingest + 3 способа query).
- [x] `perf-baseline.md` как живой snapshot (TBD-таблицы, заполняются после первого `-j Short` прогона).
- [x] **Первый честный baseline-run** — прогон завершён, `perf-baseline.md` актуализирован (commit `b157dc3`). Tier 1 CleFParser ~1.3M events/sec, KqlTransformer Where на 100k в 513μs, Tier 2 SqliteLogStore ingest ~107k events/sec на батче 100k, indexed query < 100μs независимо от fixture size. Повторять перед каждым значимым merge в hot paths + как PR-gate через `--statisticalTest 10%`.
- [ ] Stopwatch/GC.GetTotalMemory никогда — только BDN (см. антипаттерны в `performance-testing.md`).
- [ ] Tier 3 (NBomber load), memory footprint через `dotnet-counters` — когда появится hot-path; сейчас преждевременно.

## Локализация (spec §9)

- [ ] **i18n scaffold.** `IStringLocalizer` + `.resx`, dot-notation keys (`page.logs.header`), `window.__i18n` inject в `<head>` с текущей локалью для TS-кода. Хардкод user-facing строк в разметке/коде запрещён. Сейчас все строки литералом на английском, миграция в resx — когда появится второй язык либо когда накопится критическая масса (~30+ ключей).

## Отложено / tech debt после Фазы A
Накопленный долг, намеренно пропущенный ради dog-food ready.
- [x] **Hashed admin password.** `AdminPasswordHasher` (PBKDF2/HMACSHA256, 600k iter, 32-byte key, 16-byte salt), формат `v1:{iter}:{b64salt}:{b64hash}`. `AdminAuthOptions.PasswordHash` — приоритет над plaintext `Password`. CLI: `dotnet run --project YobaLog.Web -- --hash-password <plain>` печатает hash и выходит. 7 unit + 2 integration-теста.
- [x] **Copy-to-clipboard** — кнопки `⧉` у Message (inline), TraceId и Properties JSON в expanded-row. Vanilla JS через `navigator.clipboard.writeText` + короткая "copied"-индикация.
- [x] **Expandable event row** — клик по строке раскрывает hidden-row с Id/Timestamp(ISO 8601 full precision)/Level(name + rank)/Template/TraceId/SpanId/EventId + Properties JSON.
- [ ] **Edge case тесты cursor-пагинации:** duplicate timestamps, cursor на удалённую запись, boundaries (empty result, last page exactly at PageSize). Добавить при появлении багов.

## Тестовое покрытие до фазы A (что ещё нельзя пропустить)
- [x] **CLEF-парсер:** tolerant NDJSON (кривая строка не убивает батч), partial-batch ack, malformed `@t`, missing required fields. _(CleFParserTests, 20 тестов)_
- [x] **Ingestion pipeline:** Channels-based writer под нагрузкой — no event loss при `Count > capacity`; корректный shutdown-drain; per-workspace isolation. _(ChannelIngestionPipelineTests, 7 тестов)_
- [~] **API-ключи:** невалидный/missing → 401; валидный + правильный workspace → 201. **Не покрыто:** expired keys, rate-limit, unscoped key/$system access (нужна модель прав). Вернёмся при DB-migrate API-ключей в Фазе D. _(IngestionEndpointTests + ConfigApiKeyStoreTests, 12 тестов)_
- [x] **Retention executor:** удаляет ровно то, что в фильтре; идемпотентен; concurrent ingestion не блокируется; пройдёт по workspace'ам из `IWorkspaceStore`, а не по `IApiKeyStore.ConfiguredWorkspaces` (tech-debt #4 закрыт 2026-04-21). _(RetentionServiceTests, 9 тестов)_
- [x] **Cursor pagination:** базовая пагинация. **Не покрыто:** duplicate timestamps, cursor на удалённую запись, boundaries — добавить при появлении edge-case багов. _(SqliteLogStoreTests)_
- [x] **Workspace isolation:** данные из workspace A физически недостижимы запросом из B. _(SqliteLogStoreTests)_
- [x] **Маскирование:** HMAC детерминирован (один вход → один выход); удалённый share = 404; expired = 410. _(ShareEndpointTests + ValueMaskerTests + SqliteShareLinkStoreTests, 17 тестов)_

---

## Открытые вопросы

- [ ] **Производительность записи.** SQLite сериализует writes — потенциальное узкое место при высоком event rate. Плановое решение: `System.Threading.Channels` + один writer-loop per workspace с батчевой записью в WAL-режиме.
- [ ] **Обслуживание БД.** Автоматический `VACUUM` SQLite после срабатывания Retention Policy (маппится на `ILogStore.CompactAsync`). Когда запускать — после удаления >N процентов или по расписанию?
- [ ] **Покрытие KQL.**
    - MVP-операторы: `where`, `project`, `extend`, `summarize`, `count`, `take`, `order by`. `join` — позже.
    - Graceful degradation для unsupported operators: не "query failed", а "operator X not supported in yobalog, try Y".
    - Кастомные функции yobalog (`mask()` и т.п.) — через `Kusto.Language` custom function registration, либо layer поверх результата reference executor'а (решается при имплементации первой кастомной функции).
    - [x] Sync между тем, что парсится в Kusto AST, и тем, что реально работает в наших бэкендах — явный allowlist. `KqlSyntaxKindAllowlistTests`: `EverySyntaxKind_EndingInOperator_IsTriaged` бежит по `Enum.GetValues<SyntaxKind>()`, фильтрует по суффиксу `Operator`, проверяет что каждый либо в `SupportedOperators` (FilterOperator / TakeOperator / SortOperator / ProjectOperator / ExtendOperator / CountOperator / SummarizeOperator), либо в `ExplicitlyUnsupportedNames` (48 известных: JoinOperator, MvExpandOperator, ParseOperator, etc). Когда Kusto.Language ship'ит новый оператор → untriaged set непустой → fail с подсказкой "добавь в support + wire transformer или в explicit unsupported". Companion-тест `SupportedOperators_ActuallyParseWithoutUnsupportedKqlException` гоняет минимальный example query per supported-kind через Apply/Execute, чтобы поймать случай "добавили в allowlist, забыли wire в transformer-switch".
- [ ] **Props indexing policy.** Allowlist путей per-workspace через `ILogStore.DeclareIndexAsync` — единая стратегия (SQLite/DuckDB/Postgres expression-index на `json_extract`). Подвопросы: UX управления allowlist'ом; автодетект "горячих" путей по статистике запросов; защита от high-cardinality полей (UUID-ы как ключи Properties).
- [ ] **Time handling.** Клиентский `@t` в CLEF — ISO 8601 с offset. Seq верит клиенту, хранит UTC-эквивалент, не компенсирует clock skew. Повторяем это поведение. Подвопрос: UI — показывать время в TZ смотрящего или в TZ события?

---

## Рюшечки

Чистое UX-добро, не блокирует dog-food / никаких архитектурных решений. Делать, когда будет скучно.

- [ ] **Auto-detect чувствительных полей** в share-модалке: `email|ip|auth|token|password|ssn` в имени пути → предзаполнить radio как `mask`/`hide`, чтобы не клацать вручную.
- [ ] **Flat property-keys в column-дропдауне.** Сейчас column-position autocomplete показывает `Properties` как один item — двухшаговая вставка (`Properties.` → key). Можно мержить workspace-specific property keys в основной список completions на server-side, когда детектим column-expecting позицию (после `where`/`project`/`extend`/`by`/`order by`). Pro: одностадийный UX, сразу видишь `Duration` рядом с `Level`. Con: дропдаун может раздуться при большом namespace; нужен cutoff или группировка (top 20 used). Реализация: в `/api/kql/completions` endpoint — когда `PropertyKeyContext` не матчится, но Kusto предлагает column-kind items, подмешиваем `GetPropertyKeysAsync` в итоговый список с `Kind="Property"`. Существующая двухшаговая ветка остаётся fallback'ом — она работает, просто UX-нерв.
